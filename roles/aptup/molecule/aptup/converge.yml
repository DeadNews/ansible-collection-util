---
- name: Converge
  hosts: all
  become: true

  tasks:
    - name: Test role
      ansible.builtin.import_role:
        name: deadnews.util.aptup
      vars:
        aptup_next_release: false
        aptup_show_stdout: true
